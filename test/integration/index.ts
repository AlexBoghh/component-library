/**\n * Integration Tests Index\n * \n * Exports all integration tests for the Radix UI Lab system.\n */\n\nexport * from './provider-integration.test'\nexport * from './theme-integration.test'\nexport * from './component-communication.test'\n\n// Test utilities\nexport const integrationTestUtils = {\n  clearStorage: () => {\n    localStorage.clear()\n    sessionStorage.clear()\n  },\n  \n  waitForThemeTransition: async () => {\n    return new Promise(resolve => setTimeout(resolve, 600))\n  },\n  \n  mockPerformanceAPI: () => {\n    Object.defineProperty(window, 'performance', {\n      value: {\n        now: jest.fn(() => Date.now()),\n        mark: jest.fn(),\n        measure: jest.fn(),\n        getEntriesByType: jest.fn(() => []),\n      },\n      writable: true\n    })\n  },\n  \n  mockMatchMedia: () => {\n    Object.defineProperty(window, 'matchMedia', {\n      writable: true,\n      value: jest.fn().mockImplementation(query => ({\n        matches: query.includes('dark'),\n        media: query,\n        onchange: null,\n        addListener: jest.fn(),\n        removeListener: jest.fn(),\n        addEventListener: jest.fn(),\n        removeEventListener: jest.fn(),\n        dispatchEvent: jest.fn(),\n      })),\n    })\n  }\n}"